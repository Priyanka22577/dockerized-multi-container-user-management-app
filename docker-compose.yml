version: "3.8"

services:
  app:
    build: .
    ports:
      - "3007:3007"
    depends_on:
      - mongo

  mongo:
    image: mongo:6
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data: